import MessageRoom from '../components/MessageRoom' // Import the MessageRoom component
// Define the MessagesPage functional component, displays the messaging interface
const MessagesPage = () => {
   return ( 
      <MessageRoom/> // Render the MessageRoom component
    );
}
// Export the MessagesPage component
export default MessagesPage;